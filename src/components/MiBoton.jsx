// A una pieza de código que ya no se usa, se le puede llamar de diferentes maneras dependiendo del contexto. Aquí hay algunas opciones:
// Código muerto (dead code):
// Este término se usa para describir código que existe en el programa pero que nunca se ejecuta, o que se ejecuta pero sus resultados no se utilizan.
// Código legado (legacy code):
// Se refiere a código que es obsoleto y que ya no se desarrolla activamente, aunque podría ser necesario para el funcionamiento de un sistema existente.
// Código inalcanzable (unreachable code):
// Este es un tipo de código muerto que nunca puede ser ejecutado debido a la estructura del programa.
// Código obsoleto:
// Un término general para código que ya no es necesario o relevante para la funcionalidad actual del programa.
// Código redundante:
// Código que realiza la misma tarea que otro código y puede ser eliminado sin afectar la funcionalidad del programa.

// Esta pieza del código es redundante. R

// import React, { useState } from "react";

// function MiBoton() {
//   const [estaResaltado, setEstaResaltado] = useState(false);

//   const handleClick = () => {
//     setEstaResaltado(!estaResaltado);
//   };

//   return (
//     <div>
//       <button
//         className={estaResaltado ? "resaltado" : ""}
//         onClick={handleClick}
//       >
//         Haz clic aquí
//       </button>
//     </div>
//   );
// }

// export default MiBoton;
